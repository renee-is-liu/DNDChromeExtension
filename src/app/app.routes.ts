import { Routes } from '@angular/router';
import { CombatTrackerComponent } from './components/combat-tracker/combat-tracker.component';

export const routes: Routes = [
    { path: '', component: CombatTrackerComponent }
];
